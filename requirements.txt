# Project tool requirements
#
# This file documents the local tools needed to run and test the capstone
# prototype. It is intentionally not a pip-only dependency file.

# Container runtime
Docker Desktop >= 29.5
Docker Compose >= 5.1

# Gateway and test images pulled by Docker Compose / Docker run
kong:3.9
stoplight/prism:4
python:3.13-alpine
curlimages/curl:latest
grafana/k6:latest

# Local command-line tools
Git for Windows with Git Bash
curl >= 8.0
OpenSSL >= 3.0
Python >= 3.13

# Python packages used for local static checks
PyYAML >= 6.0

# Optional native install. The project can also run k6 through Docker.
k6 >= 2.0
